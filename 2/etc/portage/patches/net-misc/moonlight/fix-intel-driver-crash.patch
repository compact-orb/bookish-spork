# AI generated patch
--- a/app/main.cpp
+++ b/app/main.cpp
@@ -30,6 +30,7 @@
 #include <Windows.h>
 #elif defined(Q_OS_LINUX)
 #include <openssl/ssl.h>
+#include <dlfcn.h>
 #endif
 
 #include "cli/listapps.h"
@@ -292,6 +293,11 @@
 
 int main(int argc, char *argv[])
 {
+#if defined(Q_OS_LINUX)
+    // Hack to prevent Intel Vulkan driver from being unloaded and crashing glibc
+    dlopen("libvulkan_intel.so", RTLD_LAZY | RTLD_NODELETE);
+#endif
+
     SDL_SetMainReady();
 
     // Set the app version for the QCommandLineParser's showVersion() command
